package com.finpay.auth.entity;

/**
 * Subscription plans available during registration.
 * Determines wallet limits, feature access, and transaction caps.
 */
public enum AccountPlan {
    STARTER,
    PRO,
    ENTERPRISE
}
